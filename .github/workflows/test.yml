name: test

on:
  push:
    branches: [main]
    paths:
      - "bin/**"
      - "shell/**"
      - "commands/**"
      - "install.sh"
      - "test.sh"
      - ".github/workflows/test.yml"
  pull_request:
  workflow_dispatch:

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: shellcheck -S warning install.sh shell/msg.sh test.sh

  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.x"
      - run: ./test.sh
